class Solution {
    public int numJewelsInStones(String J, String S) {
        int cnt=0;
        for(char a:S.toCharArray())
            if(J.indexOf(a)!=-1)cnt++;
        return cnt;
        
    }
}
